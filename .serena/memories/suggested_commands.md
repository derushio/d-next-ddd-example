# 推奨コマンド一覧

## 🚀 基本開発コマンド

```bash
pnpm dev          # 開発サーバー起動（Turbopack + DB生成ウォッチ + Prisma Studio）
pnpm build        # プロダクションビルド（DB生成 → Next.jsビルド）
pnpm start        # プロダクションサーバー起動
```

**重要**: `pnpm dev` は3000ポートで起動中かを事前確認してから実行すること

## 🔍 品質チェック（推奨）

```bash
pnpm check        # 総合品質チェック（format → type-check → lint → test:unit）
```

## 🧹 個別品質チェック

```bash
pnpm lint         # ESLint実行（Next.js + テストファイル）
pnpm format       # フォーマット実行（Prettier + Markdown）
pnpm type-check   # TypeScript型チェック
pnpm clean        # ビルドキャッシュクリア
```

## 🧪 テスト関連

```bash
pnpm test         # 全テスト実行（mermaid検証 → unit → e2e）
pnpm test:unit    # ユニットテストのみ
pnpm test:e2e     # E2Eテストのみ
pnpm test:coverage # カバレッジ付きテスト
pnpm test:watch   # ウォッチモード
pnpm test:ui      # Vitest UI起動
```

## 🗃️ データベース関連

```bash
pnpm db:generate      # Prismaクライアント生成
pnpm db:studio        # Prisma Studio起動（ブラウザなし）
pnpm db:migrate:dev   # 開発用マイグレーション
pnpm db:migrate:deploy # 本番マイグレーション
pnpm db:seed          # シードデータ投入
pnpm db:push          # スキーマプッシュ
pnpm db:format        # Prismaフォーマット
```

## 🎨 shadcn/ui関連

```bash
pnpm ui:add       # UIコンポーネント追加
pnpm ui:update    # コンポーネント差分確認
pnpm ui:list      # 利用可能コンポーネント一覧
```

## 🧱 Mermaid図表関連

```bash
pnpm mermaid:validate     # Mermaid図表検証
pnpm mermaid:validate-all # 全Mermaid図表検証
pnpm mermaid:check-patterns # パターンチェック
```

## 🔧 ツール・ユーティリティ

```bash
pnpm hash:generate    # ハッシュ生成ツール
```

## 📦 システムコマンド（Linux）

```bash
ls                # ファイル・ディレクトリ一覧
cd <path>         # ディレクトリ移動
pwd               # 現在のディレクトリパス表示
find <path> -name <pattern>  # ファイル検索
rg <pattern>      # 高速テキスト検索（ripgrep）
git status        # Git状態確認
git add .         # 変更をステージング
git commit -m "message"  # コミット
git push          # リモートへプッシュ
```

## ⚡ タスク完了時の実行推奨順序

1. `pnpm format` - コードフォーマット
2. `pnpm type-check` - 型チェック
3. `pnpm lint` - リント
4. `pnpm test:unit` - ユニットテスト
5. `pnpm build` - ビルド確認（本番環境想定）

## 🎯 最も重要なコマンド

- **開発開始**: `pnpm dev`
- **品質保証**: `pnpm check`
- **テスト実行**: `pnpm test`
- **本番準備**: `pnpm build`
